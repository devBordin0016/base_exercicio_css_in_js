import React from 'react'
import { Form, Container, HeroTitle } from './HeroStyled' // Importando os styled-components

const Hero = () => (
  <Form>
    <Container>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </Container>
  </Form>
)

export default Hero
